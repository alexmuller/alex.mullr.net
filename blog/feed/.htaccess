DirectoryIndex index.xml

<IfModule mod_rewrite.c>
 RewriteEngine on
 RewriteCond %{HTTP_USER_AGENT} !FeedBurner    [NC]
 RewriteCond %{HTTP_USER_AGENT} !FeedValidator [NC]
 RewriteRule ^(.*)$ http://feeds.feedburner.com/alexmuller [R=301,NC,L]
</IfModule>
